# Claude Code — Project Instructions

## Git identity (required for verified commits)

Always run this at the start of every session before making any commits:

```bash
git config user.email "noreply@anthropic.com"
git config user.name "Claude"
```

## Branch
- Development branch: `claude/intelligent-archimedes-vxk11k`
- All work merges to `main`
- Push via `mcp__github__push_files` (local git proxy returns HTTP 503)
